// Travis Ahern
// Nov. 30, 2018

class PulseDestroyBullets {
  constructor() {
    this.img = img.destroyBullets;
  }

  pickUp(playerObj) {
    if (playerObj.item = "") {
      playerObj.item = "destroyBullets";
    }
    return playerObj;
  }
}
